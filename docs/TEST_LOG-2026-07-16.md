# Test Log — 2026-07-16

> Commands logged by machine for automation design.
> Tag format: `[machine] command`

## Setup

```
[coordinator: 590i-aorus-ultra (192.168.1.100)]
  tetron create --name test-tetronnet --hostname 590i-aorus-ultra --subnet 10.55.55.0/24
  tetron invite test-tetronnet create   →  invite-1 (for xps rejoin)
  tetron invite test-tetronnet create   →  invite-2 (for X10SRA)
  tetron invite test-tetronnet create   →  invite-3 (for xeon40)
```

### Invite manifest

| Machine       | Invite key |
|---------------|------------|
| xps-17-9720   | `4eB4pUvRL48Z29sQXwzSxqko1kwhALPpK9CueJ8ojXmybVNvj7JuSQcuqbodiXLBET` (b3100c98) — FIRST JOIN ATTEMPT WITH OLD BINARY FAILED; second invite minted below |
| xps-17-9720   | `4eB4pUvRL48Z29sQXwzSxqko1kwhALPpK9CueJ8ojXmymopHXeD11Fq5pKWqGCJ9dg` (b3100c98) — REJOIN after update |
| X10SRA        | `4eB4pUvRL48Z29sQXwzSxqko1kwhALPpK9CueJ8ojXmyYLJKT1haCk1HW3tpMaAdfX` (5b934c8c) |
| xeon40        | `4eB4pUvRL48Z29sQXwzSxqko1kwhALPpK9CueJ8ojXmykXoE2J48yX7jbfQX2bnAZa` (54b31de1) |

## Step 1 — xps-17-9720: leave, update, rejoin

```
[xps: erik@192.168.1.111]
tetron leave test-tetronnet
sudo systemctl stop tetron
```

[scp binary from coordinator to xps]

```
[xps: erik@192.168.1.111]
sudo cp /tmp/tetron /usr/local/bin/tetron
sudo systemctl start tetron
tetron join 4eB4pUvRL48Z29sQXwzSxqko1kwhALPpK9CueJ8ojXmymopHXeD11Fq5pKWqGCJ9dg --hostname xps-17-9720
tetron status
```

## Step 2 — X10SRA: install and join

```
[X10SRA: erik@192.168.1.130]
scp 590i-aorus-ultra:/usr/local/bin/tetron /tmp/tetron
sudo cp /tmp/tetron /usr/local/bin/tetron
sudo tetron install
tetron join 4eB4pUvRL48Z29sQXwzSxqko1kwhALPpK9CueJ8ojXmyYLJKT1haCk1HW3tpMaAdfX --hostname x10sra
tetron status
```

## Step 3 — xeon40: install and join (via jumphost xps)

```
[xps: erik@192.168.1.111]
scp /usr/local/bin/tetron noisebridge@100.64.0.12:/tmp/tetron
ssh noisebridge@100.64.0.12
sudo cp /tmp/tetron /usr/local/bin/tetron
sudo tetron install
tetron join 4eB4pUvRL48Z29sQXwzSxqko1kwhALPpK9CueJ8ojXmykXoE2J48yX7jbfQX2bnAZa --hostname xeon40
tetron status
```

## Step 4 — Data plane tests

```
[coordinator]
ping 10.55.55.206   # → xps
ping 10.55.55.X     # → X10SRA
ping 10.55.55.Y     # → xeon40

[xps]
ping 10.55.55.126   # → coordinator
ping 10.55.55.X     # → X10SRA
ping 10.55.55.Y     # → xeon40

[X10SRA]
ping 10.55.55.126   # → coordinator
ping 10.55.55.206   # → xps
ping 10.55.55.Y     # → xeon40

[xeon40]
ping 10.55.55.126   # → coordinator
ping 10.55.55.206   # → xps
ping 10.55.55.X     # → X10SRA
```

## Step 5 — SUBNET-BUG-001 test (new binary fa29ef9e)

**Phase 0 — Clean slate (all 4 machines):**
- Removed old binary, config, logs, runtime dirs
- Deleted stale TUN devices (`sudo ip link delete tun0` etc.)
- Installed fresh tetron 0.1.5 (fa29ef9e) on each
- Verified single `tun0` with default subnet (10.88.0.0/24)

**Phase 1 — aorus (coordinator) creates custom subnet network:**
```
tetron create --subnet 10.55.55.0/24 --name test-tetronnet
→ 10.55.55.30  invite: 4AEcThUfDhREfUdCDXH5PABNwkY2TK5VSrr4fsz7mkd2sCuJRn4oS4MbncKLePTBB9
```
`sudo tetron restart` to apply subnet to TUN.

**Phase 2 — SUBNET-BUG-001 rejection test on xps (default subnet):**
```
tetron join <invite> --hostname xps-17-9720
→ ! join failed
  node subnet is 10.88.0.0/24 but network 'test-tetronnet' uses 10.55.55.0/24;
  run `sudo tetron config set subnet 10.55.55.0/24 && sudo tetron restart`
  and try joining again
```
**SUBNET-BUG-001 PASS**

**Phase 3 — xps fixes subnet and joins:**
```
sudo tetron config set subnet 10.55.55.0/24 && sudo tetron restart
tetron join <invite> --hostname xps-17-9720
→ joined test-tetronnet  address 10.55.55.191
```
**SUBNET-BUG-001 fix allows correct join after subnet fix.**

**Phase 4 — Promote xps to co-coordinator:**
```
tetron admin test-tetronnet add c6d8cfd120
→ granted network key to c6d8cfd120
```

**Phase 5 — xps (co-coordinator) mints invites for remaining nodes:**
```
tetron invite test-tetronnet create  → invite for x10sra
tetron invite test-tetronnet create  → invite for xeon40
```

**Phase 6 — x10sra joins:**
```
sudo tetron config set subnet 10.55.55.0/24 && sudo tetron restart
tetron join <invite> --hostname x10sra
→ joined test-tetronnet  address 10.55.55.117
```

**Phase 7 — xeon40 joins (after binary re-copy, full 29MB):**
```
sudo tetron config set subnet 10.55.55.0/24 && sudo tetron restart
tetron join <invite> --hostname xeon40
→ joined test-tetronnet  address 10.55.55.183
```

## Results

| Machine | IP | Joined | Direct | Notes |
|---------|----|--------|--------|-------|
| 590i-aorus-ultra | 10.55.55.30 | coordinator | — | SUBNET-BUG-001 PASS |
| xps-17-9720 | 10.55.55.191 | yes (co-coordinator) | yes | first join rejected correctly |
| x10sra | 10.55.55.117 | yes | yes | joined correctly |
| xeon40 | 10.55.55.183 | yes | yes | joined correctly; binary was truncated (3.9MB vs 29MB) on first scp |

## Bugs Found

### SUBNET-BUG-001 — FIXED
Join with mismatched subnet now rejects with clear error. Fixed in fa29ef9.

### CONVERGE-001 — NEW: co-coordinator publish race
When a promoted co-coordinator (xps) admits new members and publishes an updated blob to the DHT, the original coordinator (aorus) may overwrite it with a stale blob (on its 300s periodic publish), causing members to disappear from both coordinators' rosters. The cascade:
1. xps publishes updated blob (members: aorus, xps, x10sra, xeon40) to DHT
2. aorus's 300s timer fires, publishes its stale blob (members: aorus, xps only)
3. xps's 60s poller sees DHT hash regressed, fetches old blob, overwrites in-memory state
4. x10sra and xeon40 vanish from both coordinators

Root cause: multiple coordinators publish to the same DHT key without coordination. The lazy publisher on co-coordinators has no `dht_notify` handle and uses polling (10s). The original coordinator's publisher overwrites the DHT on notify or 300s timer.

**Workaround**: restart the original coordinator after members join via co-coordinator.
**Fix ideas**: (a) read-before-write DHT record (fetch current hash, only publish if newer), (b) single-writer model (only original coordinator publishes to DHT), (c) co-coordinators provide blob via iroh-blobs only, never publish to DHT.

### CONVERGE-002 — NEW: stale DHT restore on coordinator restart
When aorus restarted, `restore_coordinator_network` failed with "group blob not found locally or at any seed peer" because the blob hash in the DHT pointed to bytes on xps's blob store that were inaccessible. This is a consequence of CONVERGE-001.

### TUN-CLEANUP — NEW (procedural)
Stale TUN interfaces survive `sudo tetron uninstall` and persist across install/restart cycles. On xeon40, `tun0` had old-subnet IP (10.88.134.27) while `tun1` had correct IP (10.55.55.154), causing confusion when `ip addr show tun0` was checked. Fix: Phase 1c must explicitly delete all `tun*` devices with `sudo ip link delete tunX`.

### SCP-TRUNCATION — NEW (procedural)
SCP of the 29MB binary through an SSH jump host timed out after 60s, leaving a truncated 3.9MB file on xeon40. Installing this caused a segfault on daemon start. Fix: verify binary size after scp, use longer timeout or compress with `scp -C`.

---

## Session continuation (later 2026-07-16 / early 2026-07-17 UTC) — CONVERGE-003/005/006/007, DIAL-001

Same 3 machines (xeon40 excluded per explicit instruction: "do not use xeon yet until we have a fully working tetron on the local network"). Network renamed to `converge-test`, subnet `10.66.66.0/24`. All fixes below follow the spec-first workflow: `spec/design_spec.py` entry → implement → `python3 reconcile.py` (15 gates) → deploy + verify live → commit → `libspec link`.

### Standard deploy cycle (repeated for every fix below)

```bash
# [dev machine: aorus]
cargo build --release
sudo cp target/release/tetron /usr/local/bin/tetron.new && sudo mv /usr/local/bin/tetron.new /usr/local/bin/tetron
scp -C target/release/tetron 192.168.1.111:/tmp/tetron   # xps
scp -C target/release/tetron 192.168.1.113:/tmp/tetron   # x10sra

ssh 192.168.1.111 "sudo cp /tmp/tetron /usr/local/bin/tetron.new && sudo mv /usr/local/bin/tetron.new /usr/local/bin/tetron"
ssh 192.168.1.113 "sudo cp /tmp/tetron /usr/local/bin/tetron.new && sudo mv /usr/local/bin/tetron.new /usr/local/bin/tetron"

sudo tetron restart
ssh 192.168.1.111 "sudo tetron restart"
ssh 192.168.1.113 "sudo tetron restart"
```

**Note (automation-relevant):** `cp` directly over `/usr/local/bin/tetron` fails with "Text file busy" while the daemon holds the binary open. The `cp`-to-`.new`-then-`mv` pattern is required (`rename()` doesn't care that the old inode is running) — a plain `install -m 755` would hit the same issue unless it already does this internally.

### Baseline / convergence check (repeated after every deploy)

```bash
echo "aorus:  $(sudo tetron status --json | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['networks'][0]['member_count'] if d['networks'] else 'none')")"
echo "xps:    $(ssh 192.168.1.111 "tetron status --json" | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['networks'][0]['member_count'] if d['networks'] else 'none')")"
echo "x10sra: $(ssh 192.168.1.113 "tetron status --json" | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['networks'][0]['member_count'] if d['networks'] else 'none')")"

# convergence sometimes needs one 60s poller cycle (CONVERGE-005); poll until stable:
until sudo tetron status --json | python3 -c "import json,sys; d=json.load(sys.stdin); n=d['networks'][0]; exit(0 if n['member_count']==3 else 1)"; do sleep 5; done
```

### CONVERGE-003 verification (ghost member never cleans up)

```bash
# [aorus] kick by short id (hostname resolution deliberately not accepted, see KICK-REQUIRES-ID)
sudo tetron kick converge-test <short-id>

# [x10sra] should reach networks: [] within ~1 poller cycle (was: stayed "connected" forever pre-fix)
ssh 192.168.1.113 "tetron status --json"
ssh 192.168.1.113 "ping -c 2 -W 2 <peer-mesh-ip>"   # should now fail honestly, not lie about connectivity

# rejoin to restore baseline
ssh 192.168.1.111 "tetron invite converge-test create --json"   # mint invite (co-coordinator)
ssh 192.168.1.113 "tetron join <invite-key> --hostname x10sra"
```

### CONVERGE-005 verification (generation-aware publish guard)

Reproduces the exact co-coordinator admission race that used to wedge permanently:

```bash
# [xps, co-coordinator] admit a member while aorus (original coordinator) is stale/behind
ssh 192.168.1.111 "tetron invite converge-test create --json"
ssh 192.168.1.113 "tetron join <invite-key> --hostname x10sra"

# [aorus] watch for self-convergence with NO manual restart (the actual regression test):
sudo journalctl -u tetron --no-pager -f | grep "group blob changed"
# expect: "group blob changed current_generation=N remote_generation=M" with M > N,
# followed by correct convergence within one 60s poller cycle.
```

### CONVERGE-006 verification (member boot-restore config fallback)

Simulates total DHT/pkarr unreachability across a restart:

```bash
# [x10sra] find the pkarr relay's addresses
ssh 192.168.1.113 "getent ahosts dns.iroh.link"   # e.g. 49.13.207.244, 2a01:4f8:c2c:7216::1

# [x10sra] block it, then restart with it blocked
ssh 192.168.1.113 "sudo iptables -I OUTPUT -d 49.13.207.244 -j DROP"
ssh 192.168.1.113 "sudo ip6tables -I OUTPUT -d 2a01:4f8:c2c:7216::1 -j DROP"
ssh 192.168.1.113 "sudo tetron restart"

# [x10sra] should still show the network (fallback to persisted NetworkConfig), and should
# still fully reconnect to peers via the fallback roster's coordinator identity even though
# DHT resolution itself is failing:
ssh 192.168.1.113 "tetron status --json"
ssh 192.168.1.113 "ping -c 2 <peer-mesh-ip>"

# check the fallback log line:
ssh 192.168.1.113 "sudo grep 'falling back to persisted config' /var/log/tetron/tetron.log.$(date +%F)"

# unblock — normal DHT convergence should resume on its own within one poller cycle:
ssh 192.168.1.113 "sudo iptables -D OUTPUT -d 49.13.207.244 -j DROP; sudo ip6tables -D OUTPUT -d 2a01:4f8:c2c:7216::1 -j DROP"
```

### DIAL-001 verification (background, concurrent, timeout-bounded dials)

Simulates a dead/offline roster member during both a coordinator restart and a member restart:

```bash
# [xps] simulate an offline peer
ssh 192.168.1.111 "sudo systemctl stop tetron"

# [aorus] coordinator restart with a dead roster member — status must be correct immediately,
# not blocked on the dead peer:
time sudo tetron restart
sudo tetron status --json   # member_count must already be correct within ~1-2s

# confirm the dial to the dead peer actually timed out (bounded), didn't hang:
sudo grep "dial timed out" /var/log/tetron/tetron.log.$(date +%F)   # expect timeout_secs=10

# [x10sra] member restart with a fellow member dead — reconnection to the coordinator must
# not stall on the dead peer either:
time ssh 192.168.1.113 "sudo tetron restart"
ssh 192.168.1.113 "ping -c 2 <aorus-mesh-ip>"   # should work within ~2s
ssh 192.168.1.113 "sudo grep 'mesh peer' /var/log/tetron/tetron.log.$(date +%F)"

# restore
ssh 192.168.1.111 "sudo systemctl start tetron"
```

### CONVERGE-007 verification (kick-coded close never mutates the roster)

Regression-tests both paths that changed (`CloseReason::Left` / `CloseReason::Kicked`):

```bash
# --- Left path ---
ssh 192.168.1.113 "tetron leave converge-test"
sudo tetron status --json   # member_count should drop immediately
sudo journalctl -u tetron --no-pager --since "10 seconds ago" | grep "reason=Left"

# rejoin
ssh 192.168.1.111 "tetron invite converge-test create --json"
ssh 192.168.1.113 "tetron join <invite-key> --hostname x10sra"

# --- Kicked path ---
sudo tetron kick converge-test <short-id>
sudo journalctl -u tetron --no-pager --since "10 seconds ago" | grep "reason="
# expect: kicked side logs reason=Kicked, briefly retries (falls through the reconnect
# loop's fast-skip on purpose — pruned_peers has no entry on the kicked side), then
# CONVERGE-003's poller detects the removal and leaves cleanly within ~20-30s:
ssh 192.168.1.113 "sudo journalctl -u tetron --no-pager --since '1 minute ago'" | grep -iE "removed kicked|we have been removed|leaving locally|left network"
```

### Automation notes

- Every fix above follows the same **deploy → baseline-check → provoke-the-bug-scenario → verify → restore-baseline** shape — a good candidate for a single parameterized test-runner script (machine list + IP map as config, the "provoke" step as the only per-test variable).
- `until <status check>; do sleep N; done` is the recurring pattern for "wait for convergence" — a fixed helper function (`wait_for_member_count <host> <expected_count> <timeout>`) would cover CONVERGE-003/005/006/007 and DIAL-001 alike.
- Debug-level log lines (`dial timed out`, `falling back to persisted config`, `could not dial member yet`) only appear in `/var/log/tetron/tetron.log.<date>`, not `journalctl` (console stays at `info`) — any automated log-assertion needs to grep the file, not the journal.
- `libspec` snapshot commits (`chore: libspec snapshot for <ID>`) are mechanical and always immediately follow the fix commit — safe to automate as "run libspec link, commit whatever it touched" once the fix commit lands.
